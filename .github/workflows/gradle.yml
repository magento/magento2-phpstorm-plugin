# This workflow will build a Java project with Gradle
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Run automated tests

on:
  pull_request:
    branches: [ master, '*-develop', 'mainline*' ]

jobs:
  ui-test-webstorm:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: 25
          distribution: 'temurin'
          cache: gradle
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Cache WebStorm UI test distribution
        uses: actions/cache@v4
        with:
          path: |
            out/ide-tests/cache
            out/ide-tests/installers
          key: ${{ runner.os }}-webstorm-${{ hashFiles('gradle.properties') }}
      - name: Run WebStorm UI tests
        run: xvfb-run --auto-servernum ./gradlew uiTest --no-daemon --no-build-cache --no-configuration-cache
      - name: Upload UI test failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: webstorm-ui-test-failure-artifacts
          if-no-files-found: ignore
          retention-days: 14
          path: |
            build/reports/tests/uiTest/**
            build/test-results/uiTest/**
            build/idea-sandbox/**/log/**
            build/idea-sandbox/**/system/log/**
            out/ide-tests/tests/**

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 25
        uses: actions/setup-java@v3
        with:
          java-version: 25
          distribution: 'temurin'
          cache: gradle
      - uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Run automated tests
        run: ./gradlew clean test --no-daemon --no-build-cache --no-configuration-cache

  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 25
        uses: actions/setup-java@v3
        with:
          java-version: 25
          distribution: 'temurin'
          cache: gradle
      - uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Run automated tests
        run: ./gradlew clean test --fail-fast --no-daemon --no-build-cache --no-configuration-cache
      - name: Upload Windows test failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-test-failure-artifacts
          if-no-files-found: ignore
          retention-days: 14
          path: |
            build/test-results/test/**
            build/reports/tests/test/**
            build/reports/problems/**
            build/tmp/test/**
            .intellijPlatform/sandbox/**/log-test/**
            .intellijPlatform/sandbox/**/system/log/**

  build-macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 25
        uses: actions/setup-java@v3
        with:
          java-version: 25
          distribution: 'temurin'
          cache: gradle
      - uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Run automated tests
        run: ./gradlew clean test --no-daemon --no-build-cache --no-configuration-cache

  static-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: 25
          distribution: 'temurin'
          cache: gradle
      - name: Qodana - Code Quality
        uses: JetBrains/qodana-action@v2025.3
        with:
          cache-default-branch-only: true
          use-caches: true
          args: '--baseline qodana.sarif.json'
