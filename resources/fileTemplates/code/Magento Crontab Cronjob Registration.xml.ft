#if (${CRON_GROUP})
<group id="${CRON_GROUP}">
#end
    <job name="${CRONJOB_NAME}" instance="${CRONJOB_INSTANCE}" method="execute">
        <schedule>${CRONJOB_SCHEDULE}</schedule>
    </job>
#if (${CRON_GROUP})
</group>
#end